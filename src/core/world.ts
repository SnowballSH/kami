/** The logical page. 4:3, the iPad's aspect; the renderer letterboxes it into any viewport. */
export const WORLD = { width: 1024, height: 768 } as const;

export const FIXED_STEP_MS = 1000 / 60;

export const INK_THICKNESS = 10;

export const BULLET_TIME_SCALE = 0.15;
