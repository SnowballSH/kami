export const FIXED_STEP_MS = 1000 / 60;

export const INK_THICKNESS = 10;

export const BULLET_TIME_SCALE = 0.15;

/** The board is endless, but something that drifts this far from every solid is gone for good. */
export const LOST_DISTANCE = 6000;
